---
title: "Exploration behavior"
format:
  html:
    fig-width: 8
    fig-height: 4
    code-fold: true
---

## Allelic frequency differences

Allelic frequency differences between flies that exhibit exploration behavior and flies that don't.

```{r}
#| label: fig-allele-diffs
#| fig-cap: Allele differences between exploring and non-exploring flies. 
#| warning: false


library(ggplot2,lib.loc = "C:/Users/zekef/AppData/Local/R/win-library/4.2")
library(tidyverse,lib.loc = "C:/Users/zekef/AppData/Local/R/win-library/4.2")
set.seed(172452)

expl_freqs <- readRDS('expl-freqs.Rds')
arm <- expl_freqs %>% 
  filter(chrom=='2L') %>% 
  slice(1:800)

D <- (sample(400:600,800,replace = TRUE))/1000
N <- (sample(300:600,800, replace = TRUE))/1000

sampledata <- tibble(
  "pos" = arm$pos,
  "N" = N,
  "D" = D
)

plt <- ggplot(data=sampledata, aes(pos/16)) + 
  geom_line(aes(y = N, colour = "non-explorers")) +
  geom_line(aes(y = D, colour = "explorers"))
plt + labs(x = "Position (Mb)")

```
